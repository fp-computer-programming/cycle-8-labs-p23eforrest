def count_a(word):
    num = word.count("A" and "a")
    #storing the count of the letter a in the variable num
    while 1 == 1:
        #creates while loop that always functions because 1 = 1
        return num
                
print(count_a("Haaapppy"))
#prints the count of how many letters are in this word